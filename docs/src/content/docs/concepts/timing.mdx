---
title: Timing and loops
description: Duration, delay, repeats, ping-pong, fill modes, and what happens on long frames.
---

import TimingLab from '../../../components/TimingLab.astro';
import FillModes from '../../../components/FillModes.astro';

A tween waits out its delay, plays a forward leg, and optionally returns and
repeats. Most tweens only need `Duration`, `Delay`, and an ease; the rest of this
page is for loops and edge cases.

## Loop forever

Idle motion such as bobbing or pulsing is one definition that ping-pongs forever.
It stops by itself when its node leaves the tree:

```csharp
var bob = new Tweens.Position2DY
{
    To = sprite.Position.Y - 8,
    Duration = 0.8,
    Ease = EaseType.SineInOut,
    UsePingPong = true,
    Repeats = TweenOptions.Infinite,
};
sprite.Tween(bob);
```

## Timing options

All times are double-precision seconds.

| Option | Default | Meaning |
| --- | --- | --- |
| `Duration` | `0` | Seconds per leg. Zero completes on the first eligible update. |
| `Delay` | `0` | Initial wait before the first leg. |
| `Repeats` | `0` | Cycles after the first. `TweenOptions.Infinite` (-1) repeats until cancelled; an infinite cycle that takes zero time is rejected. |
| `UsePingPong` | `false` | A forward and a backward leg form one cycle. |
| `PingPongInterval` | `0` | Wait at the far endpoint before returning. |
| `RepeatInterval` | `0` | Wait between cycles, never after the last one. |
| `Offset` | `0` | Start this many seconds into the first forward leg, within `[0, Duration]`. The delay still comes first. |

For example, `Duration = 0.5`, `UsePingPong = true`, `PingPongInterval = 0.2`,
`RepeatInterval = 0.3`, and `Repeats = 1` take 2.7 seconds: two 1.2-second cycles
and one 0.3-second gap. Drag the timeline to see each phase:

<TimingLab />

Negative and non-finite values are rejected. A long frame advances to the correct
phase, even across several cycles, without losing time at boundaries. It does not
replay the callbacks of the cycles it skipped.

## Fill and restoration

`Fill` decides what the property shows before the tween starts and after it ends.

| `FillMode` | During the initial delay | On natural completion |
| --- | --- | --- |
| `RetainFinalValue` (default) | Leave the property alone | Keep the final value |
| `ApplyFromDuringDelay` | Apply `From` | Restore the captured initial value |
| `Both` | Apply `From` | Keep the final value |
| `None` | Leave the property alone | Restore the captured initial value |

Use `Both` for staggered entrances, so items that are still waiting show their
`From` value instead of their final one.

<FillModes />

A ping-pong tween ends at its starting endpoint. Cancellation always keeps the
latest value, whatever the fill mode. Shader restoration also preserves whether an
explicit override existed; see [shader uniforms](/csharp/shaders/).

## Process and physics

`ProcessMode` defaults to `TweenProcessMode.Process`. Choose
`TweenProcessMode.Physics` to update with physics. The automatic runner uses
process and physics priority `1000`, after nodes with default priority.

`UseUnscaledTime = true` ignores `Engine.TimeScale`: process updates use
monotonic engine ticks, and physics updates use `1 / PhysicsTicksPerSecond` per
tick, which is simulation time rather than wall-clock time during catch-up.
Neither option changes pause or ownership rules.

## Progress

A handle's `Progress` is the current leg's position from 0 to 1, before easing. It
runs backwards during a ping-pong return and is not the fraction of all cycles
completed.
