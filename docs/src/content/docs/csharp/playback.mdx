---
title: Control and completion
description: Pause, resume, and cancel playback; read why a tween ended; handle callbacks, errors, and threads.
---

import PlaybackRemote from '../../../components/PlaybackRemote.astro';

Every start returns a handle. Use it to pause, resume, or cancel playback, and
await its `End` to learn why playback stopped.

Snippets run in an async Node method with an in-tree `Sprite2D` named `sprite`,
and assume `using Godot;`, `using tweens.gd;`, and `using System;`.

## Pause, resume, cancel

<PlaybackRemote />

Handles are `TweenInstance<TTarget, TValue>`. Their non-generic `TweenInstance`
base lets you keep different tween types in one collection. Cancelling keeps the
latest value; `node.CancelTweens()` cancels every tween a node owns.

## Why it ended

`End` is a shared `Task<Reason>`. Any number of callers can await it, even after
playback has ended.

| Reason | Meaning |
| --- | --- |
| `Completed` | Reached its natural end |
| `Cancelled` | `Cancel()` was called |
| `TargetFreed` | The target was disposed, freed, or queued for deletion |
| `OwnerExited` | The owner left the scene tree |
| `RunnerDisposed` | The runner, tree, or manual scheduler shut down |

Continue a sequence only on `Completed`; [sequences](/csharp/sequences/) shows
the pattern. Godot emits tree exit before it invalidates a node freed with
`Free()`, so that case may report `OwnerExited`. `QueueFree()` reports
`TargetFreed` for a node that owns its own tween.

## Cancel a wait, not the tween

`AwaitDecommissionAsync` waits with a cancellation token. The token cancels only
that wait: other waiters and playback continue unless you call `Cancel()`.

```csharp
var movement = sprite.TweenPosition(new Vector2(400, 180), 0.6);
try
{
    await movement.AwaitDecommissionAsync(cancellationToken);
}
catch (OperationCanceledException)
{
    // This wait was cancelled. Stop playback too if that is your policy.
    movement.Cancel();
}
```

Cancelling playback itself never throws `OperationCanceledException`; `End`
reports `Cancelled`.

## Callbacks

Definitions accept typed callbacks. They run in this order:

1. `OnAdd` when the tween is added.
2. `OnUpdate` with `From`, only when the fill mode applies it during the delay.
3. `OnStart` once, when playback begins.
4. `OnUpdate` once per eligible frame with the sampled value, plus once more when
   completion restores the initial value.
5. `OnEnd` on natural completion, or `OnCancel` when playback stops early.
6. `OnFinally` in every case, including faults.

The handle's terminal state is visible before terminal callbacks run, and each
terminal callback runs at most once. A long frame does not replay the callbacks
of skipped cycles. Tweens created in `OnUpdate` begin on the next update; tweens
created in `OnEnd` continue the finished tween's timeline exactly.

## Errors

An exception in interpolation, easing, a setter, or a callback faults the tween.
`End` throws when awaited, `Error` holds the exception, and several failures are
kept in an `AggregateException`. Cleanup and `OnFinally` still run, and other
tweens keep playing. Schedulers also report the error through
`UnhandledException`; the automatic runner forwards it to `GD.PushError`.

:::caution[Catch errors in `async void` callbacks]
`_Ready` and other Godot callbacks are often `async void`. Wrap their sequences in
`try`/`catch`, or a faulted tween's exception is lost.
:::

## Stay on the main thread

Create and control tweens, and await `End`, on Godot's main thread. `End` finishes
there, and ordinary Godot async code keeps its synchronization context.

- Do not block with `.Wait()` or `.Result`.
- Do not use `Task.Run` or `ConfigureAwait(false)` around engine access.
- Use Godot's `ToSignal` to wait for unrelated engine signals; tweens.gd has no
  coroutine API.
