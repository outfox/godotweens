<div class="reel not-content"><slot /></div>

<style>
	.reel {
		display: grid;
		gap: 2.5rem 2rem;
		margin-top: 1.5rem;
	}
	@media (min-width: 44rem) {
		.reel {
			grid-template-columns: 1fr 1fr;
		}
	}
	.reel :global(a) {
		color: var(--tw-mint-ink);
		text-underline-offset: 0.22em;
	}
</style>
